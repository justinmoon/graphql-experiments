# Run it

## Backend

Install deps

```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Run

```
python app.py
```

# Frontend

Install deps

```
yarn
```


Run

```
yarn start
```

The app should open in `localhost:3000`
